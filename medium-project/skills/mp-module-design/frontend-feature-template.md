# Feature 详细设计: {模块名称} — {feature 名称}

> 本文档描述 {模块名称} 的 {feature 名称} feature 的页面细节。
> 整体架构（路由结构、共享层、类型定义）见 `{模块名称}.md`。

## 1. Feature 概述

- **职责**: {一句话描述}
- **包含页面**: {页面列表}
- **对应后端模块**: {module-x}

---

## 2. 页面与路由

### {PageA} 页面

**路由**: `{/path-a}`

**功能**: {这个页面做什么}

**页面状态**:
| 状态 | 类型 | 来源 | 说明 |
|------|------|------|------|
| {state} | {type} | API / 用户输入 | {说明} |

**调用的后端接口**:
| 接口 | 触发时机 | 成功处理 | 错误处理 |
|------|---------|---------|---------|
| GET /api/xxx | 页面加载 | 渲染列表 | 显示错误提示 |
| POST /api/yyy | 表单提交 | 跳转/刷新 | 显示表单错误 |

**交互流程**:
```
用户打开页面 → 加载数据（GET /api/xxx）→ 渲染列表
用户点击创建 → 弹出表单 → 填写 → 提交（POST /api/yyy）→ 成功后刷新列表
```

**组件结构**:
```
PageA
├── SearchBar          # 搜索/筛选
├── DataTable          # 数据列表
│   └── TableRow       # 单行
├── CreateModal        # 创建弹窗
└── Pagination         # 分页
```

---

### {PageB} 页面

{同上格式}

---

## 3. Feature 私有组件

| 组件 | 用途 | 使用页面 |
|------|------|---------|
| {ComponentName} | {一句话描述} | {PageA, PageB} |

## 4. Feature 私有 Hooks

| Hook | 用途 | 使用页面 |
|------|------|---------|
| {useHookName} | {一句话描述，如: 获取{资源}列表} | {PageA} |

## 5. 消费的后端接口汇总

| 接口 | 提供方 | 调用页面 | 调用场景 |
|------|--------|---------|---------|
| GET /api/xxx | {module-x} | {PageA} | {页面加载时获取列表} |
| POST /api/yyy | {module-x} | {PageA} | {创建表单提交} |

## 6. 关键业务逻辑

### {逻辑1: 如表单校验规则 / 权限控制 / 复杂交互逻辑}
{详细描述}
