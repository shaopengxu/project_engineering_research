# {项目名称} 接口契约

> 本文档是所有模块间接口的 **single source of truth**。
> 每个接口必须包含五要素：**输入、输出、业务规则、错误处理、consumers**。
> 每个模块的接口子文件（api-contracts-{module}.md）从本文档提取生成。
>
> **构建方式**：本文档在 Step 2b 由 Architect agent 逐模块构建。首个模块会话建立通用约定，
> 后续模块会话追加各自的接口定义，最后一个模块完成后补充接口依赖矩阵和需求追溯表。
>
> **适用类型**：全栈应用（前端 SPA + 后端 API + 管理后台），以 HTTP REST API 为主格式。

## 通用约定

### 认证方式
{描述认证方式，如: Bearer Token / API Key / 无需认证}

### 响应格式
```json
// 成功
{ "data": { ... } }

// 失败
{ "error": { "code": "ERROR_CODE", "message": "描述" } }
```

### 分页约定
```json
// Request: GET /items?page=1&pageSize=20
// Response:
{
  "data": { "items": [...], "total": 100, "page": 1, "pageSize": 20 }
}
```

### HTTP 状态码
| 状态码 | 含义 |
|--------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 参数错误 |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 资源冲突 |
| 500 | 服务器错误 |

---

## {module-a} 接口

### {接口名称}

`{METHOD} {path}`

**描述**: {这个接口做什么}

**认证**: {需要 / 不需要}

**Consumers**: {列出调用此接口的模块，如: module-b, module-c}

**Request Body**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| field1 | string | 是 | {描述} |
| field2 | number | 否 | {描述，默认值: {默认值}} |

**Response 200**:
```json
{
  "data": {
    "id": "string",
    "field1": "string"
  }
}
```

**业务规则**:
- {规则1}
- {规则2}

**错误码**:
| 状态码 | error.code | 触发条件 |
|--------|-----------|---------|
| 400 | INVALID_FIELD1 | field1 格式不合法 |
| 409 | DUPLICATE_FIELD1 | field1 已存在 |

---

### {接口名称2}

`{METHOD} {path}`

{... 同上结构，每个接口都包含 Consumers 字段}

---

## {module-b} 接口

{同上结构}

---

## 模块内部接口（跨模块调用）

> 非 HTTP 暴露的接口。用于模块间 Service 层直接调用（如 module-b 的 Service 调用 module-a 的 Service）。
> 这些接口同样需要契约测试。

### {module-a}.{ServiceName}.{methodName}

**描述**: {这个方法做什么}

**Consumers**: {module-b, module-c}

**参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| param1 | string | 是 | {描述} |

**返回值**:
```typescript
// 成功
{ id: string, field1: string }

// 失败时抛出异常
throw new AppError('ERROR_CODE', 'message')
```

**业务规则**:
- {规则1}

**错误码**:
| error.code | 触发条件 |
|-----------|---------|
| NOT_FOUND | 资源不存在 |

---

## 接口依赖矩阵

> 此表用于变更影响评估。当某个接口需要修改时，查看"消费方"列确定受影响的模块。
> 技术负责人在评估接口变更时必须查阅此表。

| 接口 | 提供方 | 消费方 | 变更影响级别 |
|------|--------|--------|------------|
| {接口/方法名} | {module-a} | {module-b}, {module-c} | 高（2 个消费方） |
| {接口/方法名} | {module-b} | {module-c} | 中（1 个消费方） |
| {接口/方法名} | {module-a} | {module-d} | 中（1 个消费方） |
| {工具函数名} | shared | {module-a}, {module-b}, {module-c} | 高（所有模块） |

### 变更影响级别定义

| 级别 | 消费方数量 | 处理方式 |
|------|-----------|---------|
| 低 | 0（仅内部使用） | 提供方自行更新 |
| 中 | 1 | 提供方更新 + 通知消费方更新契约测试 |
| 高 | 2+ | 技术负责人评估影响 + 所有消费方更新契约测试和实现 |

---

## 需求追溯表

> 确保 PRD 中的每条业务规则和验收标准都已在本文档的接口定义中落地。
> 技术负责人 Review 时逐行核对：**"对应契约章节"不允许为空**。

| PRD 来源（模块 + 规则/验收标准） | 业务规则 / 验收标准原文 | 对应契约章节 | 备注 |
|-------------------------------|----------------------|------------|------|
| {PRD模块A} - 规则1 | {PRD 中的原始描述} | {module-a} 接口 > {接口名}#业务规则 | |
| {PRD模块A} - 规则2 | {PRD 中的原始描述} | {module-a} 接口 > {接口名}#错误码 | |
| {PRD模块A} - 验收标准1 | {PRD 中的原始描述} | {module-a} 接口 > {接口名}#业务规则 | |
| {PRD模块B} - 规则1 | {PRD 中的原始描述} | {module-b} 接口 > {接口名}#业务规则 | 跨模块：同时涉及 {module-a} |
| ... | ... | ... | |

---

## 模块接口子文件生成说明

> Step 3 由 Architect agent 或脚本从本文档提取生成 api-contracts-{module}.md 子文件。

### 子文件内容

每个 `api-contracts-{module}.md` 包含：

1. **本模块提供的接口**：从本文档中提取该模块的所有接口定义（完整，含业务规则和错误码）
2. **本模块消费的外部接口**：从本文档中提取该模块作为 consumer 的接口（只含签名和返回类型，不含完整业务规则）
3. **相关数据模型**：从 module-design/{module}.md 中引用

### 同步规则

- 完整版 api-contracts.md 是 single source of truth
- 子文件必须与完整版保持同步
- **完整版变更时**：技术负责人负责同步更新受影响的子文件
- 不允许直接修改子文件 — 所有变更必须先更新完整版
