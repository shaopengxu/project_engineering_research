# {项目名称} 接口契约

> 每个 Store Action / 页面交互都必须包含四要素：**输入（参数/用户操作）、输出（状态变更/UI 反馈）、业务规则、错误处理**。

## 通用约定

### 状态管理
{描述状态管理方案，如: Zustand / Pinia / Redux Toolkit / Context}

### 数据流方向
```
用户操作 → Action/Event → Store 更新 → 组件重新渲染
```

### 外部数据源（如有）
| 数据源 | 说明 |
|--------|------|
| {API base URL} | {后端服务，已有接口文档在 XXX} |
| localStorage | {本地持久化数据} |

---

## {模块A}

### Store: `{storeName}`

**描述**: {管理什么状态}

**State**:
| 字段 | 类型 | 初始值 | 说明 |
|------|------|--------|------|
| {field1} | {type} | {default} | {描述} |
| {field2} | {type} | {default} | {描述} |

**Actions**:

#### `{actionName}({params}): {returnType}`
- **描述**: {做什么}
- **参数**: {param}: {type} — {描述}
- **状态变更**: {哪些 state 字段会被修改，改成什么}
- **副作用**: {如: 调用 API、写入 localStorage、触发路由跳转}
- **业务规则**:
  - {规则1}
  - {规则2}
- **错误处理**: {失败时的状态变更和 UI 反馈}

---

### 页面: `{PageName}`

**路由**: `{/path}`

**描述**: {这个页面做什么}

**依赖 Store**: `{storeName1}`, `{storeName2}`

**核心交互**:
| 用户操作 | 触发 Action | 预期结果 |
|---------|------------|---------|
| {点击XX按钮} | {actionName} | {页面变化/跳转/提示} |
| {输入XX内容} | {actionName} | {校验结果/联想提示} |

**业务规则**:
- {规则1}
- {规则2}

**错误场景**:
| 场景 | UI 表现 |
|------|---------|
| {网络失败} | {显示错误提示，可重试} |
| {数据为空} | {显示空状态占位} |

---

## {模块B}

(同上结构)

---

## 需求追溯表

> 确保 PRD 中的每条业务规则和验收标准都已在本文档的 Store/页面定义中落地。
> 技术负责人 Review 时逐行核对：**"对应契约章节"不允许为空**，如果为空说明契约有遗漏，必须补充对应定义。

| PRD 来源（模块 + 规则/验收标准） | 业务规则 / 验收标准原文 | 对应契约章节 | 备注 |
|-------------------------------|----------------------|------------|------|
| {模块A} - 规则1 | {PRD 中的原始描述} | {Store/页面名}#业务规则 | |
| {模块A} - 规则2 | {PRD 中的原始描述} | {Store/页面名}#错误场景 | |
| {模块A} - 验收标准1 | {PRD 中的原始描述} | {Store/页面名}#核心交互 | |
| {模块B} - 规则1 | {PRD 中的原始描述} | {Store/页面名}#业务规则 | |
| ... | ... | ... | |
