# {项目名称} 接口契约

> 每个公开函数/类都必须包含四要素：**输入（参数）、输出（返回值）、业务规则、错误处理**。

## 通用约定

### 错误处理策略
{描述错误处理方式，如: 抛异常 / 返回 Result 类型 / 返回 error（Go 风格）}

### 类型导出
- 所有公开类型从 `{入口文件}` 导出
- 内部类型不导出，以 `_` 前缀或 `internal` 目录隔离

---

## {模块A}

### `{functionName}({params}): {returnType}`

**描述**: {这个函数做什么}

**参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {param1} | {type} | 是 | {描述} |
| {param2} | {type} | 否 | {描述}，默认值: {default} |

**返回值**:
```typescript
// 以 TypeScript 为例，根据实际语言调整
{
  field1: string
  field2: number
}
```

**业务规则**:
- {规则1}
- {规则2}

**异常 / 错误**:
| 错误类型 | 触发条件 |
|---------|---------|
| {ErrorClassName} | {触发条件} |
| {ErrorClassName} | {触发条件} |

**使用示例**:
```typescript
import { {functionName} } from '{package}'

const result = {functionName}({exampleParams})
```

---

### `{ClassName}`

**描述**: {这个类/结构体做什么}

**构造参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {param1} | {type} | 是 | {描述} |

**方法**:

#### `.{methodName}({params}): {returnType}`
- **描述**: {做什么}
- **参数**: {同函数参数格式}
- **返回值**: {描述}
- **业务规则**: {规则}
- **异常**: {错误类型} — {触发条件}

---

## {模块B}

(同上结构)

---

## 需求追溯表

> 确保 PRD 中的每条业务规则和验收标准都已在本文档的函数/类定义中落地。
> 技术负责人 Review 时逐行核对：**"对应契约章节"不允许为空**，如果为空说明契约有遗漏，必须补充对应接口。

| PRD 来源（模块 + 规则/验收标准） | 业务规则 / 验收标准原文 | 对应契约章节 | 备注 |
|-------------------------------|----------------------|------------|------|
| {模块A} - 规则1 | {PRD 中的原始描述} | {函数/类名}#业务规则 | |
| {模块A} - 规则2 | {PRD 中的原始描述} | {函数/类名}#异常 | |
| {模块A} - 验收标准1 | {PRD 中的原始描述} | {函数/类名}#业务规则 | |
| {模块B} - 规则1 | {PRD 中的原始描述} | {函数/类名}#业务规则 | |
| ... | ... | ... | |
