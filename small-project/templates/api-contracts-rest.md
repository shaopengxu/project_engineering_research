# {项目名称} 接口契约

> 每个接口都必须包含四要素：**输入、输出、业务规则、错误处理**。

## 通用约定

### 认证方式
{描述认证方式，如: Bearer Token / API Key / 无需认证}

### 响应格式
```json
// 成功
{ "data": { ... } }

// 失败
{ "error": { "code": "ERROR_CODE", "message": "描述" } }
```

### 分页约定（如需要）
```json
// Request: GET /items?page=1&pageSize=20
// Response:
{
  "data": { "items": [...], "total": 100, "page": 1, "pageSize": 20 }
}
```

### HTTP 状态码
| 状态码 | 含义 |
|--------|------|
| 200 | 成功 |
| 201 | 创建成功 |
| 400 | 参数错误 |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 资源冲突 |
| 500 | 服务器错误 |

---

## {模块A} 接口

### {接口名称}

`{METHOD} {path}`

**描述**: {这个接口做什么}

**认证**: {需要 / 不需要}

**Request**:
```json
{
  "field1": "string, 必填, 描述",
  "field2": "number, 可选, 描述"
}
```

**Response 200**:
```json
{
  "data": {
    "id": "string",
    "field1": "string"
  }
}
```

**业务规则**:
- {规则1}
- {规则2}

**错误码**:
| 状态码 | error.code | 触发条件 |
|--------|-----------|---------|
| 400 | INVALID_FIELD1 | field1 格式不合法 |
| 409 | DUPLICATE_FIELD1 | field1 已存在 |

---

## {模块B} 接口

(同上结构)

---

## 需求追溯表

> 确保 PRD 中的每条业务规则和验收标准都已在本文档的接口定义中落地。
> 技术负责人 Review 时逐行核对：**"对应契约章节"不允许为空**，如果为空说明契约有遗漏，必须补充对应接口。

| PRD 来源（模块 + 规则/验收标准） | 业务规则 / 验收标准原文 | 对应契约章节 | 备注 |
|-------------------------------|----------------------|------------|------|
| {模块A} - 规则1 | {PRD 中的原始描述} | {接口名称}#业务规则 | |
| {模块A} - 规则2 | {PRD 中的原始描述} | {接口名称}#错误码 | |
| {模块A} - 验收标准1 | {PRD 中的原始描述} | {接口名称}#业务规则 | |
| {模块B} - 规则1 | {PRD 中的原始描述} | {接口名称}#业务规则 | |
| ... | ... | ... | |
