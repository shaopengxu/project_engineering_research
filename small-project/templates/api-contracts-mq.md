# {项目名称} 接口契约

> 每个事件都必须包含四要素：**输入（Payload）、输出（处理结果）、业务规则、错误处理**。

## 通用约定

### 消息中间件
{描述使用的消息系统，如: RabbitMQ / Kafka / Redis Streams / SQS}

### 消息格式
```json
{
  "eventType": "string — 事件类型标识",
  "timestamp": "string — ISO 8601",
  "payload": { ... },
  "metadata": {
    "traceId": "string — 链路追踪 ID",
    "source": "string — 事件来源服务"
  }
}
```

### 消费语义
{描述消费保证，如: at-least-once / at-most-once / exactly-once}

### 重试策略
| 配置 | 值 |
|------|------|
| 最大重试次数 | {次数} |
| 重试间隔 | {策略，如: 指数退避，初始 1s} |
| 死信队列 | {队列名称} |

---

## {事件组A}

### 事件: `{EVENT_TYPE}`

**描述**: {这个事件代表什么}

**来源**: {谁发出这个事件}

**Topic / Queue**: `{topic-or-queue-name}`

**Payload**:
```json
{
  "field1": "string, 描述",
  "field2": "number, 描述"
}
```

**处理逻辑**:
1. {步骤1}
2. {步骤2}
3. {步骤3}

**业务规则**:
- {规则1}
- {规则2}

**幂等性**:
- 去重键: `{字段名}` 或 `{字段组合}`
- 重复消费时的行为: {跳过 / 更新 / 忽略}

**错误处理**:
| 错误场景 | 处理方式 |
|---------|---------|
| {场景1} | 重试 |
| {场景2} | 进入死信队列 |
| {场景3} | 记录日志，跳过 |

---

## {事件组B}

(同上结构)

---

## 需求追溯表

> 确保 PRD 中的每条业务规则和验收标准都已在本文档的事件定义中落地。
> 技术负责人 Review 时逐行核对：**"对应契约章节"不允许为空**，如果为空说明契约有遗漏，必须补充对应事件。

| PRD 来源（模块 + 规则/验收标准） | 业务规则 / 验收标准原文 | 对应契约章节 | 备注 |
|-------------------------------|----------------------|------------|------|
| {模块A} - 规则1 | {PRD 中的原始描述} | {事件名称}#业务规则 | |
| {模块A} - 规则2 | {PRD 中的原始描述} | {事件名称}#错误处理 | |
| {模块A} - 验收标准1 | {PRD 中的原始描述} | {事件名称}#处理逻辑 | |
| {模块B} - 规则1 | {PRD 中的原始描述} | {事件名称}#业务规则 | |
| ... | ... | ... | |
